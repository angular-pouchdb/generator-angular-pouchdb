language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-angular-pouchdb' ]; then cd .. && eval "mv $currentfolder generator-angular-pouchdb" && cd generator-angular-pouchdb; fi
